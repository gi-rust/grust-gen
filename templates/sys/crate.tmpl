// This file is generated by grust-gen

#![crate_name = "${crate.name}"]
#![crate_type = "lib"]

extern crate gtypes;
% for xc in sorted(extern_crates.values(), key=lambda xc: xc.name):
%     if xc.name == xc.local_name:
extern crate ${xc.name};
%     else:
extern crate ${xc.name} as ${xc.local_name};
%     endif
% endfor

use gtypes::*;
